@model IEnumerable<CEAE.Models.DTO.Answer>

@{
    ViewBag.Title = Translations.Answers;
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    <h2>@Translations.Answers</h2>
    <hr />
    <p>
        @Html.ActionLink(Translations.Create, "Create", null, new { @class = "btn btn-success" })
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Text)
                </th>
                <th>@Translations.Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Text)
                    </td>
                    <td>
                        <div class="btn-group">
                            @Html.ActionLink(Translations.Edit, "Edit", new {id = item.AnswerID}, new {@class = "btn btn-primary"})
                            @Html.ActionLink(Translations.Details, "Details", new {id = item.AnswerID}, new {@class = "btn btn-default"})
                            <a href="@Url.Action("Delete", new {id = item.AnswerID})" class="btn btn-danger">
                                <i class="fa fa-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
